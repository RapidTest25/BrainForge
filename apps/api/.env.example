# ─────────────────────────────────────────────
# BrainForge API — Environment Variables
# ─────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ─────────────────────────────────────────────

# ── Database ──────────────────────────────────
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/brainforge"

# ── Authentication ────────────────────────────
# JWT signing secrets (use strong random strings, minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET="change-me-to-a-strong-random-secret-at-least-32-chars"
JWT_REFRESH_SECRET="change-me-to-another-strong-random-secret-at-least-32-chars"

# JWT token expiration
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# ── Google OAuth (Optional) ───────────────────
# Required only if you want Google sign-in
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""

# ── Server ────────────────────────────────────
# Port for the API server
PORT=4000

# Frontend URL (for CORS and redirects)
FRONTEND_URL="http://localhost:3000"

# ── Redis (Optional) ─────────────────────────
# Required for rate limiting and real-time features
# If not set, in-memory fallback is used
REDIS_URL=""

# ── File Uploads ──────────────────────────────
# Directory for file uploads (relative to api root)
UPLOAD_DIR="./uploads"

# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# ── AI Providers (BYOK) ──────────────────────
# BrainForge uses BYOK (Bring Your Own Key) model.
# Users configure their own API keys in the app settings.
# No server-side AI keys are needed.
#
# Supported providers:
#   - OpenAI (GPT-4, GPT-3.5)
#   - Google Gemini
#   - Anthropic Claude
#   - Groq
#   - DeepSeek
#   - Mistral
